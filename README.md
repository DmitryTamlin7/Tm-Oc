# 🦀 Tm_Os: Rust Microkernel OS

![Rust](https://img.shields.io/badge/language-Rust-orange.svg?style=for-the-badge&logo=rust)
![Platform](https://img.shields.io/badge/platform-x86__64-blue.svg?style=for-the-badge)
![Status](https://img.shields.io/badge/status-active-success.svg?style=for-the-badge)
![License](https://img.shields.io/badge/license-MIT-yellow.svg?style=for-the-badge)

**Tm_Os** — это современная учебная операционная система, написанная на языке Rust для архитектуры x86_64. 
Проект ориентирован на изучение низкоуровневых механизмов управления памятью, обработки прерываний и построения безопасной архитектуры ядра без использования стандартной библиотеки (no_std).
---

## 🚀 Ключевые особенности (Core Features)

* **🛡️ Memory Safety**: Полная защита от dangling pointers и race conditions благодаря системе владения Rust.
* **🖥️ Advanced VGA Driver**: Драйвер текстового режима с поддержкой цветов, скроллинга и «умным» выводом времени в реальном времени.
* **⚙️ Interrupt Handling**: Сложная система обработки прерываний (IDT), включая Page Faults, Breakpoints и защищенный Double Fault стек.
* **🧠 Memory Management**:
* 4-уровневая система таблиц страниц (Paging).
* Динамическая куча (Heap) на базе **Linked List Allocator**.
* Использование стандартных коллекций: `Vec`, `Box`, `String`, `BTreeMap`.


* **⏳ Async Executor**: Кооперативная многозадачность на основе **Futures** и **Async/Await** — современный подход в разработке ядер.
* **📂 RamFS**: Встроенная файловая система (read-only) для хранения системных файлов прямо в образе ядра.

---

## 🏗 Архитектура системы

Проект разделен на логические слои для обеспечения модульности:

| Модуль | Описание |
| --- | --- |
| `vga_buffer` | Интерфейс вывода. Реализован `try_lock`, предотвращающий **Deadlocks** в прерываниях. |
| `interrupts` | Конфигурация **PICS 8259**, обработка аппаратных тиков таймера и клавиатуры. |
| `memory` | Аллокатор фреймов и управление таблицами страниц `x86_64`. |
| `task` | Асинхронный исполнитель (Executor) и обработка очередей скан-кодов. |
| `fs` | Простейшая файловая система (RamFS) с поддержкой поиска и чтения. |

---

## 🧠 Технический стек

### Управление временем и ресурсами

Система использует **PIT (Programmable Interval Timer)**, настроенный на частоту 1000 Гц. Атомарный счетчик тиков позволяет реализовать асинхронный `sleep`, не блокируя выполнение других задач ядра.

### Асинхронный Shell

Вместо классического цикла ожидания, Tm_Os использует событийную модель:

1. **Hardware**: Клавиша нажата -> IRQ 1 -> Скан-код в очередь.
2. **Kernel**: Пробуждение задачи (Waking up) через `AtomicWaker`.
3. **Executor**: Обработка только тогда, когда есть данные. Процессор отдыхает (`hlt`), если задач нет.

---

## 🖥 Командная оболочка (Shell)

Интерактивная консоль с поддержкой системных утилит:

```bash
> help       # Список доступных команд
> ls         # Просмотр файлов во встроенной RamFS
> cat <file> # Вывод содержимого текстового файла
> hexdump    # Просмотр бинарных данных файла
> uptime     # Время работы системы (ticks/ms)
> sum <num>   # сумма всех чисел до
> info # инфо о системе

И ТД.

```

---

## ⚡ Быстрый старт

### Требования

* Rust nightly канал
* Компоненты: `rust-src`, `llvm-tools-preview`
* Эмулятор: **QEMU**
* Утилиты: `cargo install bootimage`

### Запуск

```bash
# Клонировать репозиторий
git clone https://github.com/your-repo/tm_os.git
cd tm_os

# Собрать образ и запустить в QEMU
cargo run

```

---

## 📈 Дорожная карта (Roadmap)

* [x] **Phase 1-2**: VGA, GDT, IDT (Фундамент системы).
* [x] **Phase 3-4**: Paging & Frame Allocator (Управление RAM).
* [x] **Phase 5-6**: Heap Allocator & `alloc` Support.
* [x] **Phase 7-8**: Hardware Interrupts (Timer, Keyboard).
* [x] **Phase 9**: **Async/Await Executor** (Современная многозадачность).
* [x] **Phase 10**: **RamFS** & Extended Shell (Filesystem & Utils).
---

<p align="center">
<b>Разработано  на чистом Rust.</b> 🦀


---